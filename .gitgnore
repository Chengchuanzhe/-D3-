# --- Python 相关 ---
# 忽略 Python 虚拟环境（重中之重！）
.venv/
venv/
ENV/
# 忽略 Python 编译产生的临时垃圾
__pycache__/
*.py[cod]

# --- Mac 系统相关 ---
# 忽略 Mac 自动生成的桌面配置文件（别让它污染仓库）
.DS_Store

# --- 数据文件相关（可选，请仔细阅读下方的警告！） ---
# 警告：GitHub 单个文件限制 100MB（50MB就开始警告）。
# 你的 ocean_data.nc 是海气耦合全球数据，体积可能非常大。
# 如果它超过 100MB，上传必报错。
# 如果是大文件，建议使用 Git LFS 或者放在网盘里，并在 README 里附上链接。
# 如果想忽略大型 netCDF 数据文件，请把下面这行的 # 号去掉：
# *.nc

# 忽略 Word/Excel 等 Office 软件产生的临时文件
~$*